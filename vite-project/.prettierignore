index.html
dist
node_modules
test-results
playwright-report
# Rust-часть: сборка и сгенерированные Tauri файлы; Cargo.toml и .rs форматирует cargo fmt
src-tauri
package-lock.json
# Наборы перенесены из временных скриптов дословно; отформатируем при переписывании на локаторы Playwright
tests/e2e/ported
package.json
# Стили оставлены в авторском (компактном) виде — иначе Prettier раздувает их в тысячи строк
src/styles
